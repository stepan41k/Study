<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.FlashMorze" parent="Theme.Material3.DayNight.NoActionBar">

        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.FlashMorze" parent="Base.Theme.FlashMorze" />

    <style name="Widget.FlashMorze.Button" parent="Widget.Material3.Button">
        <!-- Цвета -->
        <item name="backgroundTint">@color/grey</item>
        <item name="rippleColor">@color/grey</item>
        <item name="android:textColor">@color/white</item>

        <!-- Форма -->
        <item name="cornerRadius">16dp</item>
        <item name="strokeColor">@color/white</item>
        <item name="strokeWidth">2dp</item>

        <!-- Текст -->
        <item name="android:textAllCaps">false</item>
        <item name="android:textSize">18sp</item>

        <!-- Тень (elevation) -->
        <item name="android:elevation">6dp</item>
    </style>

</resources>